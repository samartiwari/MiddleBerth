# IntelliJ
.idea/
*.iml
*.iws
*.ipr

# Build output
target/
build/

# OS
.DS_Store

# Local env / secrets. The glob matters: a ".env.bak-1789299785" left by an
# editor or a script is the same secrets in a file git does not ignore, and
# "git add -A" will happily stage it.
.env
.env.*
*.local

# Personal notes — design reasoning and learning, not part of the codebase
CLAUDE.md
initial.md
zenith-idea-shelved.md
LEARNING.md
STEPS.md

# Docker compose local overrides
docker-compose.override.yml

# Load test output: per-request data runs to tens of megabytes a level.
loadtest/results/
